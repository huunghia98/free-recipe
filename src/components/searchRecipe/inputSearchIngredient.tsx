import React from 'react';

type Props = {
  initVal: string;
  setValue: (i: string) => void;
};

const InputSearchIngredient = ({ initVal, setValue }: Props) => {
  return <div>I</div>;
};

export default InputSearchIngredient;
